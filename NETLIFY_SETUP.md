# راهنمای Deploy در Netlify

## 🚀 مراحل Deploy

### 1️⃣ آماده‌سازی Gmail

قبل از deploy، باید یک **App Password** از Gmail دریافت کنید:

1. به [Google Account Security](https://myaccount.google.com/security) بروید
2. **2-Step Verification** را فعال کنید (اگر فعال نیست)
3. به [App Passwords](https://myaccount.google.com/apppasswords) بروید
4. در قسمت "Select app" گزینه **Mail** را انتخاب کنید
5. در قسمت "Select device" گزینه **Other** را انتخاب کنید و نام دلخواه بنویسید (مثلاً "Netlify Form")
6. روی **Generate** کلیک کنید
7. پسورد 16 رقمی را کپی کنید (این پسورد فقط یکبار نشان داده میشود!)

---

### 2️⃣ Push کردن کد به GitHub

\`\`\`bash
# اگر قبلاً git init نکرده‌اید:
git init

# اضافه کردن تمام فایل‌ها
git add .

# Commit اول
git commit -m "Initial commit: Contact form with Netlify Functions"

# تعیین برنچ اصلی
git branch -M main

# اضافه کردن remote (آدرس repository گیتهاب خودتان)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Push کردن
git push -u origin main
\`\`\`

---

### 3️⃣ اتصال به Netlify

1. به [Netlify.com](https://app.netlify.com) بروید
2. اگر اکانت ندارید با GitHub ثبت‌نام کنید
3. روی **Add new site** کلیک کنید
4. **Import an existing project** را انتخاب کنید
5. **Deploy with GitHub** را انتخاب کنید
6. Repository خود را پیدا کنید و انتخاب کنید

---

### 4️⃣ تنظیمات Build

Netlify باید خودکار این تنظیمات را تشخیص دهد (از `netlify.toml`):

- **Build command**: \`npm run build\`
- **Publish directory**: \`dist\`
- **Functions directory**: \`netlify/functions\`

اگر خودکار تشخیص نداد، دستی وارد کنید.

روی **Deploy site** کلیک کنید.

---

### 5️⃣ تنظیم Environment Variables (مهم! ⚠️)

بعد از اولین deploy:

1. به **Site settings** بروید
2. از منوی سمت چپ **Environment variables** را انتخاب کنید
3. روی **Add a variable** کلیک کنید و این 3 متغیر را اضافه کنید:

| Key | Value | مثال |
|-----|-------|------|
| **EMAIL_USER** | ایمیل Gmail شما | `your.email@gmail.com` |
| **EMAIL_PASSWORD** | App Password که از Gmail گرفتید | `abcd efgh ijkl mnop` |
| **ADMIN_EMAIL** | ایمیل ثابت شما (که فرم‌ها به آن ارسال شود) | `your.email@gmail.com` |

**نکته مهم**: 
- `EMAIL_USER` و `ADMIN_EMAIL` معمولاً یکسان هستند (ایمیل خودتان)
- `EMAIL_PASSWORD` باید **App Password** باشه، نه پسورد اصلی Gmail!

---

### 6️⃣ Redeploy سایت

بعد از اضافه کردن Environment Variables:

1. به تب **Deploys** بروید
2. روی **Trigger deploy** کلیک کنید
3. **Deploy site** را انتخاب کنید

---

### 7️⃣ تست کردن

بعد از deploy:

1. به آدرس سایت خود بروید (مثلاً `https://your-site-name.netlify.app`)
2. فرم را پر کنید
3. چک کنید:
   - ✅ ایمیل به ایمیل شما رسید؟
   - ✅ ایمیل تاییدیه به کاربر رسید؟
   - ✅ فایل Excel به ایمیل‌ها attach شده؟

---

## 🔍 عیب‌یابی (Troubleshooting)

### مشکل: ایمیل ارسال نمیشه

1. **چک کنید Environment Variables**:
   - به **Site settings** → **Environment variables** بروید
   - مطمئن شوید هر 3 متغیر وجود دارند
   - مطمئن شوید `EMAIL_PASSWORD` همان App Password است

2. **چک کنید Gmail 2-Step Verification**:
   - باید فعال باشه وگرنه App Password کار نمیکنه

3. **بررسی Logs**:
   - به **Deploys** → آخرین deploy → **Functions** بروید
   - روی `send-email` کلیک کنید
   - لاگ‌های خطا را بررسی کنید

### مشکل: فانکشن پیدا نمیشه (404)

1. مطمئن شوید فولدر `netlify/functions/send-email.js` وجود دارد
2. مطمئن شوید `netlify.toml` در ریشه پروژه است
3. Redeploy کنید

### مشکل: پسورد Gmail اشتباهه

- پسورد باید **App Password** باشه نه پسورد اصلی
- App Password 16 رقمیه (با فاصله یا بدون فاصله)
- اگر پسورد رو گم کردید، یکی جدید بسازید

---

## 📊 مانیتورینگ

### چک کردن استفاده از کوتا:

1. به **Site settings** → **Usage** بروید
2. بخش **Functions** را بررسی کنید:
   - **Requests**: تعداد دفعات استفاده (125K رایگان)
   - **Runtime**: زمان اجرا (100 ساعت رایگان)

### لاگ‌های Function:

1. به **Functions** → **send-email** بروید
2. لیست تمام اجراها را میبینید
3. روی هر کدوم کلیک کنید تا جزئیات ببینید

---

## 🎨 Custom Domain (اختیاری)

اگر میخواهید دامین شخصی وصل کنید:

1. به **Domain settings** بروید
2. **Add custom domain** را کلیک کنید
3. دامین خود را وارد کنید
4. DNS records را طبق راهنمایی Netlify تنظیم کنید

---

## 🔐 امنیت

- ✅ همیشه از App Password استفاده کنید (نه پسورد اصلی)
- ✅ Environment Variables در Netlify رمزنگاری شده‌اند
- ✅ هیچ وقت پسوردها را در کد commit نکنید
- ✅ فایل `.env` در `.gitignore` قرار دارد

---

## 📞 پشتیبانی

اگر مشکلی داشتید:

1. [مستندات Netlify Functions](https://docs.netlify.com/functions/overview/)
2. [مستندات Nodemailer](https://nodemailer.com/about/)
3. [تنظیمات Gmail App Passwords](https://support.google.com/accounts/answer/185833)

---

**موفق باشید! 🎉**

